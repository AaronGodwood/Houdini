% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{houdini_validate}
\alias{houdini_validate}
\title{Check a configuration before generating a document}
\usage{
houdini_validate(
  input_doc,
  input_sheet,
  file_location,
  figure_location = NULL,
  quiet = FALSE
)
}
\arguments{
\item{input_doc}{Path to the template (or previously generated) .docx}

\item{input_sheet}{Path to a config .xlsx (see \code{\link[=read_xlsx]{read_xlsx()}}) or a
data.frame with Bookmark/Table columns plus optional filter columns}

\item{file_location}{Folder containing the table .rtf files named in the config}

\item{figure_location}{Folder containing the figure .rtf files named in the config (defaults to table location)}

\item{quiet}{Suppress the printed summary; the findings are always returned}
}
\value{
Invisibly, a data.frame of findings with one row per problem and
columns \code{row}, \code{severity}, \code{bookmark}, \code{table}, \code{message} and \code{hint}. A
clean config returns a zero-row data.frame.
}
\description{
Runs every check the app shows in its validation panel, without opening the
app and without writing anything. Use it in a script or CI step to fail
early, with the whole list of problems at once, rather than discovering them
one at a time during a run.
}
\details{
The same rules drive the app's warnings panel, so a config that validates
cleanly here will validate cleanly there.
}
\examples{
\dontrun{
# Fail a CI step on any error
problems <- houdini_validate("report.docx", "config.xlsx", "tables/")
if (any(problems$severity == "error")) stop("config is not ready")
}
}
