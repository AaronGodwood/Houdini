% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parsing.R
\name{extract_pngs}
\alias{extract_pngs}
\title{Extract every PNG image from an RTF file, one per page}
\usage{
extract_pngs(path, parameters = NULL)
}
\arguments{
\item{path}{Path to the .rtf file}

\item{parameters}{Optional character vector of parameter values to keep.
Pages whose parameter is NA are always kept, matching filter_pages().}
}
\value{
list(images, warnings) where images is a list of
list(png_bytes, width_twips, height_twips, parameter), possibly empty
}
\description{
A figure RTF holds one \code{\\pict} group per \code{\\sectd} page, and each
page carries its own "Parameter: \if{html}{\out{<value>}}" line in the page header, exactly as
a table RTF does. Pages are returned in document order so the caller can
filter them by parameter and insert what remains.
}
