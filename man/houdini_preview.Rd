% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preview.R
\name{houdini_preview}
\alias{houdini_preview}
\title{Preview a single RTF table or figure}
\usage{
houdini_preview(
  path,
  parameters = NULL,
  timelines = NULL,
  levels = NULL,
  excluded_cols = NULL,
  excluded_rows = NULL,
  hide_data = FALSE,
  browse = interactive(),
  file = tempfile(fileext = ".html")
)
}
\arguments{
\item{path}{Path to the .rtf file}

\item{parameters}{Character vector of parameter values to keep (NULL = all)}

\item{timelines}{Character vector of timepoint labels to keep (NULL = all)}

\item{levels}{Character vector of indent level titles to keep (NULL = all)}

\item{excluded_cols}{Integer vector of 1-based column indices to drop}

\item{excluded_rows}{Integer vector of data-row IDs to drop}

\item{hide_data}{Replace values with XX, for sharing a layout without data}

\item{browse}{Open the page in a browser or the RStudio viewer. When FALSE
the file is written but not opened.}

\item{file}{Where to write the HTML. Defaults to a temporary file.}
}
\value{
Invisibly, the path to the HTML file
}
\description{
Renders one RTF file to a standalone HTML page and opens it, without
starting the Shiny app. Useful for checking how a table parses, confirming
that a filter selects what you expect, or inspecting a file that failed
during a run.
}
\details{
Filters accept the same values as [apparate()]'s config columns, so a
selection can be tried here before being committed to a workbook.
}
\examples{
\dontrun{
houdini_preview("tables/t_14_1_1.rtf")
houdini_preview("tables/t_14_1_1.rtf", parameters = "Cholesterol")
}
}
