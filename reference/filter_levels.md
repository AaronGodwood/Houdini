# Filter rows by indent level value

Rows with no content are always kept.

## Usage

``` r
filter_levels(combined, levels)
```

## Arguments

- combined:

  Output of combine_pages()

- levels:

  Character vector of level indent titles to include
