# Filter not found

Filter not found

## Usage

``` r
warn_filter_not_found(filter_name, filter_type = NULL)
```

## Arguments

- filter_name:

  The filter that cannot be found

- filter_type:

  The filter type (expected to be parameter or timepoint)
