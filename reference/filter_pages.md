# Filter pages by parameter value

Pages with NA parameter are always kept.

## Usage

``` r
filter_pages(pages, parameters)
```

## Arguments

- pages:

  Output of parse_rtf()

- parameters:

  Character vector of parameter values to include
