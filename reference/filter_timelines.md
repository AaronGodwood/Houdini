# Filter rows by timeline value

Rows with NA timeline are always kept.

## Usage

``` r
filter_timelines(combined, timelines)
```

## Arguments

- combined:

  Output of combine_pages()

- timelines:

  Character vector of timeline values to include
