# Format an error for display in the UI (message + hint on a new line if present)

Format an error for display in the UI (message + hint on a new line if
present)

## Usage

``` r
err_format(e)
```

## Arguments

- e:

  A condition object
