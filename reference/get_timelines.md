# Get all unique timeline values across pages

Get all unique timeline values across pages

## Usage

``` r
get_timelines(combined)
```

## Arguments

- combined:

  Output of combine_pages()

## Value

Character vector of unique timeline values
